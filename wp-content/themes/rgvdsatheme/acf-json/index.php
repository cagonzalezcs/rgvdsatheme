<?php
// Silence is golden. ACF Local JSON save point — field groups edited in
// wp-admin auto-save here (and load from here) so definitions stay in the
// theme, never DB-only. PHP-registered groups in inc/ are unaffected.
