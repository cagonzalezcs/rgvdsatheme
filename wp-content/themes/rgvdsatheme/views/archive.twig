{# Category/date archives render the same BlogArchive island as the blog home;
   archive.php computes `title` and inc/blog.php injects the archive payload. #}
{% extends "index.twig" %}
{% set page_header_props = {
	title: title,
	crumbs: [{ label: 'Home', href: '/' }]
} %}
